from django.urls import path

from .views import *

app_name = 'pos'

urlpatterns = [
    # path('product/create', .as_view()),
    # path('product/delete', .as_view()),
    # path('product/put', .as_view()),
    # path('product/list/', .as_view()),
    # path('order/create/', .as_view()),
    # path('order/delete/', .as_view()),
    # path('order/put/', .as_view()),
    # path('order/list/', .as_view()),
    # path('student/performance/<int:pk>', .as_view()),
]
